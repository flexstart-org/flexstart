[**Live →**](https://docs.miteshgupta.com)

## Local Development

First, run `yarn install` or `pnpm i` to install the dependencies.

Then, run `yarn dev` or `pnpm dev` to start the development server and visit localhost:3000.
